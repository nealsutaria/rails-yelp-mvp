<%= simple_form_for [@restaurant, @review] do |f| %>
  <div>
  <%= f.label :rating %>
  <%= f.text_field :rating %>
</div>

<div>
  <%= f.label :content %>
  <%= f.text_field :content %>
</div>

  <%= f.submit %>
<% end %>

<%= link_to "Back", restaurant_path(@restaurant) %>
